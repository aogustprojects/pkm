CREATE TABLE IF NOT EXISTS `poli_gigi_rujukan` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `name` VARCHAR(255) NOT NULL,
  `no_rm` VARCHAR(255) NOT NULL,
  `tempat_lahir` VARCHAR(255) NOT NULL,
  `tanggal_lahir` DATE NOT NULL,
  `umur` INT NOT NULL,
  `alamat` VARCHAR(255) NOT NULL,
  `petugas_pel` VARCHAR(255) NOT NULL,
  `pemberi_info` VARCHAR(255) NOT NULL,
  `penerima_info` VARCHAR(255) NOT NULL,
  `diagnosis` VARCHAR(255),
  `nama_tujuan_tindakan` VARCHAR(255),
  `indikasi_tindakan` VARCHAR(255),
  `tata_cara` VARCHAR(255),
  `risiko_komplikasi` VARCHAR(255),
  `prognosis` VARCHAR(255),
  `alternatif_risiko` VARCHAR(255),
  `biaya_tindakan` VARCHAR(255),
  `signature1` TEXT,
  `signature2` TEXT,
  `jenis_kelamin` VARCHAR(50) NOT NULL,
  `tanggal` DATE NOT NULL,
  `jam` TIME NOT NULL,
  `persetujuan_tindakan` VARCHAR(255) NOT NULL,
  `created_at` DATETIME DEFAULT NULL,
  `updated_at` DATETIME DEFAULT NULL,
  `signature3` TEXT,
  `signature4` TEXT,
  `signature5` TEXT
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
